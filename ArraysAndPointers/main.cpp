#include <iostream>
using namespace std;

void printNumbers(int numbers[], int size) {
    size(numbers);
}

int main() {
    int numbers[] = {1, 2, 3};
    int* ptr = numbers;
    cout << ptr[1];
    return 0;
}

